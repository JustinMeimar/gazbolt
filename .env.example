# Backend URI for frontend to connect to
# For development: http://localhost:8000
# For docker-compose: http://backend:8000 (default)
VITE_BACKEND_URI=http://localhost:8000

# Port to expose the backend on the host
BACKEND_PORT=8000